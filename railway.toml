[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["src/**", "package.json", "tsconfig.json", "vite.config.ts"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
healthcheckPath = "/api/meetings/dates"
healthcheckTimeout = 30
