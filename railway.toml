[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "node --import tsx src/server/index.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
healthcheckPath = "/api/meetings/dates"
healthcheckTimeout = 30
