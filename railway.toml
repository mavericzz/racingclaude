[build]
builder = "nixpacks"
buildCommand = "rm -rf node_modules/.cache && npm ci && npm run build"

[deploy]
startCommand = "NODE_ENV=production node --import tsx src/server/index.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
