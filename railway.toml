[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "NODE_ENV=production node --loader tsx src/server/index.ts"
healthcheckPath = "/api/meetings?date=2026-03-09"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
