FROM node:22-slim

WORKDIR /app

# Install deps
COPY package.json package-lock.json ./
RUN npm ci --include=dev

# Copy source
COPY . .

ENV NODE_ENV=production

# Run the daily pipeline (meetings + runners + scraper + results)
CMD ["node", "--import", "tsx", "src/scripts/dailyPipeline.ts"]
